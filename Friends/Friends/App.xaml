<?xml version="1.0" encoding="utf-8" ?>
<Application xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Friends.App">
    <Application.Resources>
        <ResourceDictionary>
            <!-- Colors -->
            <!-- Please update Android styles.xml and iOS AppDelegate.cs -->
            <Color x:Key="Primary">#3f1dcb</Color>
            <Color x:Key="PrimaryLight">#7c4dff</Color>
            <Color x:Key="BarBackground">#ffffff</Color>
            <Color x:Key="Cancel">Gray</Color>

            <!-- Label -->
            <Style x:Key="labelHeader" TargetType="Label">
                <Setter Property="HorizontalOptions" Value="StartAndExpand"/>
                <Setter Property="FontSize" Value="20"/>
                <Setter Property="TextColor" Value="{StaticResource PrimaryLight}"/>
            </Style>
            
            <!-- ImageButton -->
            <Style x:Key="ibtnHeader" TargetType="ImageButton">
                <Setter Property="BackgroundColor" Value="Transparent"/>
                <Setter Property="Opacity" Value="0.5"/>
                <Setter Property="WidthRequest" Value="40"/>
                <Setter Property="HeightRequest" Value="40"/>
                <Setter Property="Padding" Value="5"/>
            </Style>
            <Style x:Key="ibtnTab" TargetType="ImageButton">
                <Setter Property="BackgroundColor" Value="Transparent"/>
                <Setter Property="WidthRequest" Value="60"/>
                <Setter Property="HeightRequest" Value="60"/>
                <Setter Property="Padding" Value="15"/>
                <Setter Property="HorizontalOptions" Value="CenterAndExpand"/>
                <Setter Property="VerticalOptions" Value="Center"/>
            </Style>
            
            <!-- Frame -->
            <Style x:Key="frameViewContainer" TargetType="Frame">
                <Setter Property="HorizontalOptions" Value="FillAndExpand"/>
                <Setter Property="VerticalOptions" Value="FillAndExpand"/>
                <Setter Property="BackgroundColor" Value="Transparent"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="Margin" Value="0"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>